{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "(Windows) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/guesscmd.exe",
            "args": ["-input","ncs","${config:insfile}"],
            "stopAtEntry": false,
            "cwd": "${config:out_folder}",
            "environment": [],
            "console": "externalTerminal"
        }
    ]
}
